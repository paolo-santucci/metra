<?xml version="1.0" encoding="utf-8"?>
<!-- API 31+ data extraction rules.
     Excludes all app data from cloud backup and device-to-device transfer.
     The DB key lives in EncryptedSharedPreferences (Android Keystore), the DB
     is SQLCipher-encrypted — but if both were included in a backup the pair
     would travel together. Exclude both to prevent key+DB exfiltration. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
