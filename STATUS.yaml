schema-version: 2
last-updated: "2026-06-04T22:22:09+02:00"
active-plan: null
last-plan: ".claude/docs/plans/archive/qp-20260525-backup-restore-toast.md"

# Owned by the short-plan skill. update-status preserves verbatim.
short-plan:
  feature: null
  slug: null
  auto: false
  request-type: null
  workflow: null
  parent:
    spec: null
    plan: null
    milestone: null
    frs: null
  paths:
    spec: null
    plan: null
    assess-dir: null
  phases:
    assess: pending
    triage: pending
    spec: pending
    plan: pending
    build: pending
  approvals:
    spec: null
    plan: null
  rejection-note: null
  build-progress:
    current-wave: null
    waves-completed: []

# Owned by the long-plan skill. update-status preserves verbatim.
long-plan:
  feature: "implement issues: #11, #12, #13, #16, #18, #20, #21, #22, #28, #29, #31, #32, #33"
  slug: "implement-next-patch"
  auto: false
  request-type: bug-fix
  paths:
    spec: ".claude/docs/specs/lp-20260517-implement-next-patch-spec.md"
    plan: ".claude/docs/plans/lp-20260517-implement-next-patch-plan.md"
    assess-dir: ".claude/docs/specs/lp-20260517-implement-next-patch-assessment"
  phases:
    assess: done
    spec: done
    plan: done
  approvals:
    spec: approved
    plan: approved

pending-consolidation: []

recently-completed:
  - date: "2026-06-03"
    summary: "fix(android): keep app package in ProGuard — prevent R8 from stripping MainActivity under proguard-android-optimize.txt (74300a7)"
  - date: "2026-06-03"
    summary: "fix(android): add Tink+security-crypto ProGuard rules, KeepAliveService keep rule, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permission — fixes release APK cold-start crash (36052f9)"
  - date: "2026-06-03"
    summary: "fix(quality): replace deprecated SizeTransition.axisAlignment; suppress todo analyzer warnings (e4ed6c4)"
  - date: "2026-06-03"
    summary: "fix(l10n): sync ARB sources with restoreSuccessToast dart fix — restoreSuccessToast reads 'entries' in both ARB and generated dart (f660e21)"
  - date: "2026-06-01"
    summary: "fix(backup): wipe stale passphrase on connect — prevent false autoBackupActive (BUG-B06); bump pubspec to 1.0.2+13; tagged v1.0.2-rc19 (616e34d)"

in-progress:
  - title: "Ship rollback/rc10 → TestFlight as v1.0.2-rc20"
    state: "Reconciliation with main ABANDONED — main's 26 commits (rc11–17) did NOT fix the iOS black screen; rollback/rc10 (rollback to old deps: secure_storage 9.x, flutter_web_auth_2 4.1.0, file_picker 8.x) is the candidate fix. TestFlight builds now come from rollback/rc10 only. Local rc10 was 6 commits ahead of origin (ProGuard fixes unpushed)."
    touched:
      - ".github/workflows/ios.yml"
      - "STATUS.yaml"
    next-step: "Push rollback/rc10 to origin, tag v1.0.2-rc20, push tag → ios.yml deploy_testflight builds IPA (build#=GITHUB_RUN_NUMBER) + uploads to TestFlight. Then user device-tests iOS + Android."
    blocker: null

next-steps:
  - "Device-test v1.0.2-rc20 (rollback/rc10 baseline): iOS via TestFlight — confirm black screen on cold-start with backup configured is GONE"
  - "Build + sideload Android release APK from rollback/rc10 locally (android.yml only builds from main) — confirm cold-start works (ProGuard/Tink fixes)"
  - "If rc20 verified stable on both platforms: promote rollback/rc10 to canonical (fast-forward or replace main), then tag v1.0.2 final"
  - "Re-attempt main's dependency upgrades (KGP phase 2: secure_storage 10.x, file_picker 11.x, timezone 5.x) only AFTER 1.0.2 ships — they did not fix the black screen and reintroduced it"
  - "FR-16 TestFlight sign-off: cold-start with iOS notification permission revoked → in-app toggle reflects 'off'"
  - "Consider adding universal fat APK build to CI alongside split APKs for easier sideload testing distribution"
  - "Screenshot refresh for backup-connected IT+EN (deferred — requires Android emulator with IT/EN locale)"

known-issues:
  - severity: critical
    description: "iOS cold-start black screen (backup configured) was NOT fixed by main's 26 commits (rc11–17: SQLCipher fallback, flutter_web_auth_2 downgrade, GoRouter try/catch, Keychain entitlements). rollback/rc10 was branched to roll back the KGP phase-2 dep upgrade (back to secure_storage 9.x, flutter_web_auth_2 4.1.0, file_picker 8.x) — this is the candidate fix, shipping as v1.0.2-rc20 to TestFlight. Reconciliation/merge of main into rollback/rc10 is ABANDONED (main's fixes are ineffective)."
    impact: high
    workaround: "Release line = rollback/rc10 only. Build + ship to TestFlight from rollback/rc10 via v* tag. Do not re-apply main's dep upgrades until 1.0.2 ships and black screen is confirmed gone."
  - severity: warning
    description: "share_plus and package_info_plus still bundle KGP; share_plus 12.x has Kotlin K2 compilation bug; unblocks when file_picker 12.x is stable"
    impact: low
    workaround: "Accept KGP warnings on every build; auto-resolves when file_picker 12.x is stable"
  - severity: warning
    description: "save_daily_log_test.dart future date rejection — intermittent failure near Italian midnight UTC (timezone/DST boundary)"
    impact: medium
    workaround: null
  - severity: warning
    description: "Screenshot refresh for backup-connected IT+EN deferred — requires Android emulator with IT/EN locale"
    impact: low
    workaround: "Old screenshots remain in help docs; text is correct; refresh at next manual QA session"
  - severity: warning
    description: "iOS PermissionDenied vs PermissionBlocked indistinguishable via flutter_local_notifications v17 — both return PermissionBlocked"
    impact: low
    workaround: "Conservative UX: settings dialog always offered on iOS denial; slightly over-eager on first-time denials"
