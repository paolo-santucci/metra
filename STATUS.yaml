schema-version: 2
last-updated: "2026-06-04T22:22:09+02:00"
active-plan: null
last-plan: ".claude/docs/plans/archive/qp-20260525-backup-restore-toast.md"

# Owned by the short-plan skill. update-status preserves verbatim.
short-plan:
  feature: null
  slug: null
  auto: false
  request-type: null
  workflow: null
  parent:
    spec: null
    plan: null
    milestone: null
    frs: null
  paths:
    spec: null
    plan: null
    assess-dir: null
  phases:
    assess: pending
    triage: pending
    spec: pending
    plan: pending
    build: pending
  approvals:
    spec: null
    plan: null
  rejection-note: null
  build-progress:
    current-wave: null
    waves-completed: []

# Owned by the long-plan skill. update-status preserves verbatim.
long-plan:
  feature: "implement issues: #11, #12, #13, #16, #18, #20, #21, #22, #28, #29, #31, #32, #33"
  slug: "implement-next-patch"
  auto: false
  request-type: bug-fix
  paths:
    spec: ".claude/docs/specs/lp-20260517-implement-next-patch-spec.md"
    plan: ".claude/docs/plans/lp-20260517-implement-next-patch-plan.md"
    assess-dir: ".claude/docs/specs/lp-20260517-implement-next-patch-assessment"
  phases:
    assess: done
    spec: done
    plan: done
  approvals:
    spec: approved
    plan: approved

pending-consolidation: []

recently-completed:
  - date: "2026-06-03"
    summary: "fix(android): keep app package in ProGuard — prevent R8 from stripping MainActivity under proguard-android-optimize.txt (74300a7)"
  - date: "2026-06-03"
    summary: "fix(android): add Tink+security-crypto ProGuard rules, KeepAliveService keep rule, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permission — fixes release APK cold-start crash (36052f9)"
  - date: "2026-06-03"
    summary: "fix(quality): replace deprecated SizeTransition.axisAlignment; suppress todo analyzer warnings (e4ed6c4)"
  - date: "2026-06-03"
    summary: "fix(l10n): sync ARB sources with restoreSuccessToast dart fix — restoreSuccessToast reads 'entries' in both ARB and generated dart (f660e21)"
  - date: "2026-06-01"
    summary: "fix(backup): wipe stale passphrase on connect — prevent false autoBackupActive (BUG-B06); bump pubspec to 1.0.2+13; tagged v1.0.2-rc19 (616e34d)"

in-progress:
  - title: "main is now canonical (== old rollback/rc10); iOS black screen still to verify"
    state: "main REPLACED with rollback/rc10 via fast-forward (origin/main had already been moved onto the rc10 line to eef96ca, so no force-push needed; old iOS commits preserved via v1.0.2-rc17 + earlier tags). main @469489d = working build: Android startup crash fixed (kotlin-android plugin applied), verified on Galaxy A32. rollback/rc10 branch deleted (redundant). android.yml trigger narrowed to main + v* tags + dispatch."
    touched:
      - "android/app/build.gradle.kts"
      - ".github/workflows/android.yml"
    next-step: "Verify iOS rc20 TestFlight 1.0.2 (320) cold-start black screen (backup configured) is GONE — the last open blocker. When confirmed, cut a clean tag from main (v1.0.2-rc21 or v1.0.2 final) → one v* tag builds iOS TestFlight + Android signed artifacts."
    blocker: "Awaiting iOS black-screen verification on TestFlight (Android crash fixed; main consolidated)"

next-steps:
  - "Verify iOS rc20 TestFlight 1.0.2 (320) cold-start black screen (backup configured) is GONE — last open blocker before v1.0.2 final"
  - "When iOS verified: tag v1.0.2 (or rc21) from main → v* trigger builds iOS TestFlight + Android signed split-APKs + AAB in one shot"
  - "Re-attempt dependency upgrades (KGP phase 2: secure_storage 10.x, file_picker 11.x, timezone 5.x) only AFTER 1.0.2 ships — they did not fix the black screen and reintroduced it"
  - "FR-16 TestFlight sign-off: cold-start with iOS notification permission revoked → in-app toggle reflects 'off'"
  - "Consider adding universal fat APK build to CI alongside split APKs for easier sideload testing distribution"
  - "Screenshot refresh for backup-connected IT+EN (deferred — requires Android emulator with IT/EN locale)"

known-issues:
  - severity: critical
    description: "Android startup crash (ClassNotFoundException MainActivity) on rollback/rc10 — FIXED. Real cause: org.jetbrains.kotlin.android was declared in settings.gradle.kts but never applied in app/build.gradle.kts, and android.builtInKotlin=false, so MainActivity.kt was never compiled into the APK. The -keep ProGuard rule (74300a7) was a misdiagnosis. Fix applied + verified on Galaxy A32. SEPARATE open item: iOS cold-start black screen (backup configured) — rollback/rc10 reverts the KGP-2 dep upgrade as candidate fix; still pending TestFlight device verification (rc20 build 320)."
    impact: high
    workaround: "Release line = rollback/rc10 only. Android fix committed; CI rebuild produces working signed APKs. iOS black screen still to confirm on device before tagging v1.0.2 final."
  - severity: warning
    description: "share_plus and package_info_plus still bundle KGP; share_plus 12.x has Kotlin K2 compilation bug; unblocks when file_picker 12.x is stable"
    impact: low
    workaround: "Accept KGP warnings on every build; auto-resolves when file_picker 12.x is stable"
  - severity: warning
    description: "save_daily_log_test.dart future date rejection — intermittent failure near Italian midnight UTC (timezone/DST boundary)"
    impact: medium
    workaround: null
  - severity: warning
    description: "Screenshot refresh for backup-connected IT+EN deferred — requires Android emulator with IT/EN locale"
    impact: low
    workaround: "Old screenshots remain in help docs; text is correct; refresh at next manual QA session"
  - severity: warning
    description: "iOS PermissionDenied vs PermissionBlocked indistinguishable via flutter_local_notifications v17 — both return PermissionBlocked"
    impact: low
    workaround: "Conservative UX: settings dialog always offered on iOS denial; slightly over-eager on first-time denials"
