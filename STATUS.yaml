schema-version: 2
last-updated: "2026-07-05T17:15:00+02:00"
active-plan: null
last-plan: ".claude/docs/plans/archive/qp-20260629-gdrive-riverpod-ref-after-drift-mutation.md"

# Owned by the short-plan skill. update-status preserves verbatim.
short-plan:
  feature: null
  slug: null
  auto: false
  request-type: null
  workflow: null
  parent:
    spec: null
    plan: null
    milestone: null
    frs: null
  paths:
    spec: null
    plan: null
    assess-dir: null
  phases:
    brainstorm: pending
    assess: pending
    triage: pending
    spec: pending
    plan: pending
    build: pending
  approvals:
    spec: null
    plan: null
  rejection-note: null
  build-progress:
    current-wave: null
    waves-completed: []

# Owned by the long-plan skill. update-status preserves verbatim.
long-plan:
  feature: |
    Extend Métra cloud backup beyond Dropbox to Google Drive (cross-platform,
    OAuth, drive.file scope, visible "Metra" folder) and iCloud (iOS/iPadOS
    only, icloud_storage plugin + container entitlement, hidden on Android),
    same rules as Dropbox (zero-knowledge AES-256-GCM + Argon2id, app-folder
    isolation, retention=3, 507 progressive-prune). Single active provider at
    a time (new activeProvider field; SyncProvider enum += googleDrive,iCloud);
    switching = confirm → revoke old token → connect new → fresh chain;
    passphrase REUSED across providers; old provider .enc files LEFT intact;
    existing Dropbox users unaffected (additive DB migration). UI: CupertinoPicker
    wheel in bottom sheet (both platforms) with Connect button, on empty view
    and reachable from connected view for switching. Language: follow-system
    fallback resolves Italian only for Italian system locale (incl. it-CH),
    English for every other language (update _effectiveLangCode + MaterialApp
    localeResolutionCallback); explicit Settings choice unaffected. Docs: new
    internal dev guide .claude/docs/guides/cloud-provider-setup.md (not on Jekyll
    site) for Google Drive + iCloud credential provisioning + Dropbox recap.
  slug: "cloud-backup-google-drive-icloud"
  auto: false
  request-type: feature
  paths:
    spec: ".claude/docs/specs/lp-20260624-cloud-backup-google-drive-icloud-spec.md"
    plan: ".claude/docs/plans/lp-20260624-cloud-backup-google-drive-icloud-plan.md"
    assess-dir: ".claude/docs/specs/lp-20260624-cloud-backup-google-drive-icloud-assessment"
  phases:
    brainstorm: done
    assess: done
    spec: done
    plan: done
  approvals:
    spec: approved

pending-consolidation:
  - plan-id: sp-20260624-m1-provider-abstraction-foundation-spec
    archived-date: "2026-06-24"
    has-spec-impact: true
  - plan-id: sp-20260624-m2-google-drive-provider-spec
    archived-date: "2026-06-24"
    has-spec-impact: true
  - plan-id: sp-20260625-m3-icloud-provider-spec
    archived-date: "2026-06-25"
    has-spec-impact: true
  - plan-id: sp-20260625-m4-picker-switch-flow-spec
    archived-date: "2026-06-26"
    has-spec-impact: true
  - plan-id: qp-20260627-m5-locale-fallback-fix
    archived-date: "2026-06-27"
    has-spec-impact: true
  - plan-id: qp-20260627-google-drive-android-only
    archived-date: "2026-06-27"
    has-spec-impact: true
  - plan-id: qp-20260627-icloud-eventual-consistency-fix
    archived-date: "2026-06-28"
    has-spec-impact: true
  - plan-id: qp-20260628-icloud-restore-fix-picker-size-label
    archived-date: "2026-06-28"
    has-spec-impact: true

recently-completed:
  - date: "2026-07-05"
    summary: "Google Drive OAuth E2E WORKING on Android emulator. 3 fixes COMMITTED+PUSHED on metra-1.1.0: 71bc55e redirect delivery (scheme-only intent-filter, path-style URI has no host + MainActivity dual-scheme kOAuthSchemes), d49a979 account email via GET /drive/v3/about (drive.file grants it; no identity scopes, OQ-05 intact), 171ea46 _switchInFlight guard (build() holds BackupRunning(switching) during mid-switch Drift re-emissions; I-09 test). Console-side: 'Enable custom URI scheme' REQUIRED on Android OAuth client."
  - date: "2026-07-05"
    summary: "v1.1.0-rc4 tagged+pushed (bump 1.1.0+19, 88387d0) -> TestFlight CI run 28738213676. Calendar 5th-of-month test flake fixed (17dda19). Suite 1858 pass/4 skip/0 fail."
  - date: "2026-06-29"
    summary: "BUG-01/02/03 fixes COMMITTED+PUSHED 13c5e96 on metra-1.1.0. BUG-01 (HIGH): ref.read(resolveBackupProvider) hoisted before Drift mutations in switchProvider(). Suite 1854 pass/4 skip/0 fail."
  - date: "2026-06-28"
    summary: "iCloud restore download-completion fix + restore-picker '<date> <time> <size>' label COMMITTED 861c327 + bump 1.1.0+18 (d1ea254) + v1.1.0-rc3 tagged/pushed (TestFlight CI run 28326882713)."
  - date: "2026-06-28"
    summary: "iCloud backup fixes COMMITTED 50e18d1 + v1.1.0-rc2 tagged/pushed. BUG-1: eventual-consistency false-failure fixed. BUG-2: Disconnect no-op fixed."

in-progress:
  - title: "v1.1.0-rc4 iOS TestFlight (run 28738213676) — CI in progress"
    state: "Tagged v1.1.0-rc4 on 88387d0 (1.1.0+19). Carries GDrive OAuth fixes + switch-flicker guard + prior rc3 iCloud fixes."
    touched:
      - "android/app/src/main/AndroidManifest.xml"
      - "lib/data/services/backup/google_drive_provider.dart"
      - "lib/features/backup/state/backup_notifier.dart"
    next-step: "Confirm CI deploy_testflight green; then on-device verify rc4 (iCloud restore + picker labels, carried from rc3)."
    blocker: null
  - title: "metra-1.1.0 — all LP milestones done; awaiting PR into main"
    state: "M1–M5 + platform matrix + iCloud/GDrive fixes all pushed. Tags rc1..rc4. pending-consolidation 8 entries."
    touched:
      - "lib/features/backup/state/backup_notifier.dart"
      - "lib/core/l10n/locale_resolver.dart"
    next-step: "Open metra-1.1.0 -> main PR after rc4 on-device verify + E2E Drive round-trip gate."
    blocker: null

next-steps:
  - "Run manual E2E Drive round-trip pre-merge gate (OQ-QA-05): back up to Google Drive -> download .enc -> verify unreadable binary -> restore on a 2nd Android device -> data identical."
  - "Add GOOGLE_OAUTH_CLIENT_ID GitHub secret if not yet set (CI builds pass it via --dart-define on 5 build commands); local client now works with custom-URI-scheme enabled."
  - "Consolidate canon AFTER metra-1.1.0 merges: re-derive backup slice via canon-bootstrap (M1-M4), apply M5 settings delta via spec-changelog, fresh /finalize-plan. pending-consolidation has 8 entries."
  - "After GitHub Pages rebuilds verify privacy policy live at metra.paolosantucci.com/{it,en}/privacy/, then resubmit to Google Play to clear privacy-policy rejection."
  - "Download signed release-aab from Android CI run on main (d702ab3) and upload to Play Console (versionCode 12) to retry production promotion (16 KB page-size fix)."
  - "GitHub issue #33 (notification cancel-then-reschedule has no atomicity test against the Doze mid-window alarm race) is a genuine unimplemented gap -- canon notifications.md Gap 3 still open. Needs an actual fix/test, unlike #20/21/22/28/29/32 which were verified already-fixed in code and closed 2026-07-05."
  - "settings.md (Gaps 9-11) and onboarding.md (Gaps 5, 7) were manually edited 2026-07-05 to drop stale gap entries after confirming code fixes for closed issues #20/21/22/28/29 -- still needs a formal canon-bootstrap re-derivation, same as the pending M1-M5 backup consolidation above."

known-issues:
  - severity: warning
    description: "iCloud eventual-consistency family (FIXED IN CODE, pending on-device re-verify via rc3/rc4 TestFlight): backup false-failure + Disconnect no-op fixed in 50e18d1/rc2; Restore PathNotFoundException fixed in 861c327/rc3."
    impact: medium
    workaround: "Pre-fix builds: backup succeeded despite false error; restore required re-triggering after file materialized."
  - severity: warning
    description: "Drift DB/SQLCipher tests need native sqlite3 lib absent in local distrobox; CI ubuntu has the lib."
    impact: low
    workaround: "mkdir -p /tmp/sqlitelib && ln -sf /usr/lib/x86_64-linux-gnu/libsqlite3.so.0 /tmp/sqlitelib/libsqlite3.so then LD_LIBRARY_PATH=/tmp/sqlitelib flutter test"
  - severity: warning
    description: "share_plus and package_info_plus still bundle KGP; share_plus 12.x has Kotlin K2 compilation bug; unblocks when file_picker 12.x is stable."
    impact: low
    workaround: "Accept KGP warnings on every build; auto-resolves when file_picker 12.x is stable."
  - severity: warning
    description: "save_daily_log_test.dart future date rejection — intermittent failure near Italian midnight UTC (timezone/DST boundary)."
    impact: medium
    workaround: null
